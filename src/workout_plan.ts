import { Logbook, ExerciseDefinitionSet } from "./logbook";

export class WorkoutPlan {

}

export function generatePlan(logbook: Logbook, exercises: ExerciseDefinitionSet): WorkoutPlan {
  // TODO
  return new WorkoutPlan;
}
